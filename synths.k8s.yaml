apiVersion: v1
kind: Service
metadata:
  name: synths-service-c8e5f5c4
  namespace: default
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: goodbye-jenkins
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: synths-deployment-c8a911dc
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: goodbye-jenkins
  template:
    metadata:
      labels:
        app: goodbye-jenkins
    spec:
      containers:
        - image: paulbouwer/hello-kubernetes:1.8
          name: hello-kubernetes
          ports:
            - containerPort: 8080
